<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base TV theme — no action bar, black background -->
    <style name="Theme.TVApp" parent="@style/Theme.Leanback">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorPrimary">@color/black</item>
        <item name="android:colorPrimaryDark">@color/black</item>
        <item name="android:colorAccent">@color/white</item>
    </style>

    <!-- Fullscreen variant applied to MainActivity -->
    <style name="Theme.TVApp.Fullscreen" parent="Theme.TVApp">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Keep screen on while the TV app is running -->
        <item name="android:keepScreenOn">true</item>
    </style>

</resources>
